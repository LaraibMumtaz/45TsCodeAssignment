// Array of animals with a common characteristic
let animals = ['Dog', 'Cat', 'Rabbit'];

// Print each animal name using a for loop
for (let animal of animals) {
    console.log(`A ${animal.toLowerCase()} have four legs.`);
}

// Additional line outside the loop
console.log(`Any of these animals are having 4 legs!`);
