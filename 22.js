// Create an array to store a list of fruits
let fruits = ["Apple", "Banana", "Cherry", "Date", "Elderberry"];
// Intentionally accessing an out-of-bounds index to produce an error
console.log("Accessing a non-existent index:");
export {};
//console.log(fruits[-10]); // This will cause an index error as the index 10 does not exist
